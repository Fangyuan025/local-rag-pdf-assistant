# --- Document Parsing ---
docling>=2.0.0

# --- Local LLM Engine ---
# Pinned to 0.3.19 because that is the latest version with a prebuilt
# Windows + Python 3.12 wheel in abetlen's index. Install with:
#   CPU:    pip install "llama-cpp-python==0.3.19" --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu --only-binary=:all:
#   CUDA:   pip install "llama-cpp-python==0.3.19" --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121 --only-binary=:all:
llama-cpp-python==0.3.19

# --- Embeddings & Vector Store ---
sentence-transformers>=2.7.0
chromadb>=0.5.0

# --- Orchestration ---
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.0

# --- Evaluation (Offline) ---
ragas>=0.2.0
datasets>=2.20.0

# --- Frontend ---
streamlit>=1.36.0

# --- Utilities ---
pydantic>=2.7.0
python-dotenv>=1.0.1
pandas>=2.2.0
